startCommand:
  type: http
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        title: "Novada API Key"
        description: "Your Novada API key from app.novada.com/dashboard. Get one free at novada.com. Leave empty to use the free tier."
    required: []
  exampleConfig:
    apiKey: "your-api-key-here"
