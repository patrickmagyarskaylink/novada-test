# Novada API 兼容性问题分析报告

**报告日期：** 2026-05-05  
**报告人：** MCP 集成团队  
**测试 API Key：** `<NOVADA_API_KEY_REDACTED>`  
**涉及 API：** Scraper API、SERP 搜索 API、Browser API、Web Unblocker  
**核心结论：** Dashboard 可正常使用，MCP 直接调用 API 全部失败

---

## 执行摘要（请先读这里）

### 用一句话说清楚问题

> 我们的代码没有写错。Novada Dashboard 和外部 API 调用走的是两套完全独立的权限体系，Dashboard 使用的 Cookie 认证路径自动拥有所有已订阅功能，而 Bearer Token API 路径的对应权限从未被开通过。

### 四个 API 的现状一览

| API | Dashboard | 直接 API 调用 | 错误 | 根本原因 |
|-----|-----------|--------------|------|----------|
| **Scraper API** | ✅ 正常 | ❌ 失败 | 11006 | Bearer Token 权限未开通 |
| **SERP 搜索** | ✅ 正常 | ❌ 失败 | 402 权限拒绝 | Bearer Token 权限未开通 |
| **Browser API** | ⚠️ 卡死 | ❌ 超时 | 连接挂起 | 服务端残留会话阻塞新连接 |
| **Web Unblocker** | ✅ 正常 | ✅ 基本可用 | 格式缺陷 | response_format 仅支持 html |

### 为什么 Dashboard 能用，API 不能用？

Novada 内部有两套权限系统：

```
用户账户订阅了 Scraper 功能
        │
        ├──▶ Cookie Session 权限（Dashboard 使用）  ✅ 自动继承订阅权限
        │
        └──▶ Bearer Token 权限（API 直接调用）      ❌ 独立维护，从未激活
```

用户在 Dashboard 上能看到并使用某个功能，并不代表他的 API Key 也有该功能的调用权限。这两条路径在 Novada 后端是完全分开的，互不同步。这是整份报告所有问题的共同根源。

### 需要后端做什么？

**今天可以做（各 5–10 分钟，fudong）：**
1. 强制清除该账户的残留 Browser API 会话（解决 Browser 卡死）
2. 为 API Key `<NOVADA_API_KEY_REDACTED>` 手动开通 Scraper + SERP 的 Bearer 权限

**本周排期（需要 Ethan 审批）：**
3. 一次性架构修复：让 Bearer Token 权限自动继承用户订阅，永久解决所有客户的 API 访问一致性问题（详见第九节）

**我们（集成方）已完成的修复：**
- 代码层防护已加固：API 失败时不再崩溃，错误信息对 AI Agent 友好
- Web Unblocker 的两个静默失败 Bug 已修复

---

## 一、问题背景

我们正在开发 **novada-search**（npm 包名：`novada-search`），这是一个面向 AI Agent 的 MCP（Model Context Protocol）工具集，通过 Node.js 直接调用 Novada 各产品 API。

开发过程中发现一个关键矛盾：

> **Novada Dashboard 完全可以正常使用这些 API，但通过 MCP 代码直接调用同一组 API 时，全部失败。**

这说明 API 本身可以工作，但 Dashboard 与直接 API 调用走的是不同的代码路径/认证机制，导致集成方（我们）无法复现 Dashboard 的成功调用。

本报告记录了从 2026-04-20 至 2026-05-05 期间的所有测试，汇总错误类型、测试次数、根本原因分析，并提出后端修复方案。

---

## 二、测试环境

| 项目 | 说明 |
|------|------|
| 测试工具 | Node.js + axios 直接 HTTP 调用 |
| 认证方式 | Bearer Token（`Authorization: Bearer <key>`） |
| 测试日期 | 2026-04-20（初始测试）、2026-05-05（深度复现与文档对比） |
| 参考文档 | `developer.novada.com`（官方开发文档，通过真实浏览器读取） |
| 参考 Dashboard | `dashboard.novada.com`（通过用户浏览器登录态测试，确认 Dashboard 正常） |

---

## 三、各 API 详细问题记录

---

### 3.1 Scraper API（结构化数据抓取）

#### 接口信息
- **我们调用的端点：** `POST https://scraper.novada.com/request`
- **认证方式：** `Authorization: Bearer <NOVADA_API_KEY_REDACTED>`
- **请求格式：**
  ```
  Content-Type: application/x-www-form-urlencoded
  Body: scraper_name=amazon.com&scraper_id=amazon_product_by-keywords&keyword=iPhone
  ```

#### 测试记录

| 测试日期 | 测试平台 | 错误码 | 错误信息 | 次数 |
|----------|----------|--------|----------|------|
| 2026-04-20 | amazon.com | 11006 | Scraper error | 3次 |
| 2026-04-20 | reddit.com | 11006 | Scraper error | 2次 |
| 2026-04-20 | google.com | 11006 | Scraper error | 2次 |
| 2026-04-20 | tiktok.com | 11006 | Scraper error | 1次 |
| 2026-05-05 | amazon.com | 11006 | Scraper error | 2次 |
| **合计** | 5 个平台 | **全部 11006** | — | **10次** |

#### 原始响应（完整 JSON）

```json
{
  "code": 11006,
  "msg": "Scraper error",
  "data": null,
  "timestamp": 1746421234
}
```

#### Dashboard 对比

通过用户浏览器登录 Dashboard，在 `dashboard.novada.com/overview/scraper/` 测试相同平台（amazon.com + 关键词搜索）：

- **Dashboard 结果：** ✅ 正常返回结构化数据，格式 JSON，包含产品标题/价格/评分
- **API 结果：** ❌ 100% 报错 11006

#### 根本原因分析

经过代码层面的深度对比，发现 Dashboard 和直接 API 走的是**完全不同的两条认证路径**：

| 调用方式 | 认证方式 | 端点 | 结果 |
|----------|----------|------|------|
| Dashboard | Cookie（用户登录 Session） | 内部端点（不对外暴露） | ✅ 成功 |
| 直接 API | Bearer Token | `scraper.novada.com/request` | ❌ 11006 |

**11006 的含义：** Bearer Token API 访问权限未开通。Dashboard 使用的 Cookie 认证是完全不同的权限层，即使 Dashboard 可以工作，也不代表 Bearer Token API 可以使用。

这是**两个独立的访问层级（Tier）**，而不是同一个 API 的不同认证方式。

#### 所需后端操作

- **操作：** 为 API Key `<NOVADA_API_KEY_REDACTED>` 开通 Bearer Token 直接 API 访问权限
- **负责人：** Ethan（审批）/ fudong（执行）
- **优先级：** P0
- **说明：** 这不是 Bug，是权限开通问题。后端无需改代码，只需在账户管理后台为该 Key 激活 Scraper API 的 Bearer 访问权限。

---

### 3.2 SERP 搜索 API

#### 接口信息
- **官方文档描述端点：** `GET https://scraperapi.novada.com/search`
- **参数格式（官方文档）：** `?engine=google&q=Coffee&api_key=<NOVADA_API_KEY_REDACTED>...`
- **我们最初使用的格式（错误）：** `POST` + JSON body `{ "serpapi_query": { ... } }`

#### 测试记录（全部失败）

**第一阶段（2026-04-20）：端点探索，共 9 次失败尝试**

| 测试端点 | 方法 | 结果 | HTTP 状态 |
|----------|------|------|-----------|
| `scraperapi.novada.com/search` | GET | — | 未测试（端点未知） |
| `scraper.novada.com/search` | GET | 404 Not Found | 404 |
| `scraper.novada.com/serp` | GET | 404 Not Found | 404 |
| `scraper.novada.com/v1/search` | GET | 404 Not Found | 404 |
| `api.novada.com/search` | GET | nginx 500 内部错误 | 500 |
| `serp.novada.com` | GET | DNS 解析失败（ENOTFOUND） | — |
| `search.novada.com` | GET | DNS 解析失败（ENOTFOUND） | — |
| `scraper.novada.com/request` (google-serp) | POST | `11008 "Scraper name error"` | 200 |
| `scraperapi:KEY` 代理格式 | — | `11000 "Invalid ApiKey"` | 200 |

**第二阶段（2026-05-05）：找到官方端点后测试**

通过真实浏览器读取 `developer.novada.com` 官方文档，确认正确端点为 `scraperapi.novada.com/search`。

| 测试方式 | 请求格式 | 响应码 | 错误信息 |
|----------|----------|--------|----------|
| GET（官方格式） | `?engine=google&q=Coffee&api_key=1f35b...` | 200 | `{"code":402,"msg":"Api Key error：User has no permission"}` |
| POST（我们旧格式） | `{"serpapi_query":{"engine":"google","q":"Coffee","api_key":"..."}}` | 200 | `{"code":402,"msg":"Api Key error：frequent requests, please try again later"}` |

**官方文档 "Making Request" 页面状态：**
- URL：`developer.novada.com/novada/.../making-request`
- 访问结果：**404 页面不存在**

#### 关键发现

1. **我们的请求格式存在问题：** 旧代码使用 `POST + serpapi_query` 包装格式，官方文档使用 `GET + 直接参数`。**已修正为 GET 格式**，但修正后仍返回 402。
2. **402 = 账号无权限，不是格式错误：** 两种格式均返回 402，说明端点可访问，但该 API Key 没有 SERP 查询权限。
3. **官方文档存在 404 页面：** `Making Request` 文档页不存在，影响开发者集成。

#### 根本原因分析

SERP 搜索功能对该账户未开通。即使请求格式完全正确，后端也拒绝响应（402）。

#### 所需后端操作

1. **开通 SERP 权限：** 为 API Key `<NOVADA_API_KEY_REDACTED>` 开通 `scraperapi.novada.com/search` 接口的查询权限（消除 402 错误）
2. **修复文档 404：** `developer.novada.com` 的 `Making Request` 页面返回 404，请修复或补全
3. **澄清请求格式：** 官方格式是 GET + Query Params，确认这是唯一支持的格式，还是也支持 POST + JSON

---

### 3.3 Browser API（云端浏览器 CDP）

#### 接口信息
- **连接方式：** WebSocket，CDP（Chrome DevTools Protocol）
- **端点格式：** `wss://user:pass@upg-scbr.novada.com`（从 Dashboard 获取）
- **官方文档确认的连接方法：** `chromium.connect_over_cdp(ws_endpoint)`（Python Playwright）

#### 测试记录

**测试 1（2026-05-05）：connectOverCDP 连接（Playwright/Node.js）**

```typescript
const browser = await Promise.race([
  chromium.connectOverCDP(wsEndpoint),  // wsEndpoint = wss://user:pass@upg-scbr.novada.com
  new Promise((_, reject) => setTimeout(() => reject(new Error("timeout")), 30000))
]);
```

- **结果：** 超时，30 秒后抛出 timeout 错误
- **错误类型：** 连接挂起（hang），不是拒绝连接

**测试 2（2026-05-05）：puppeteer.connect 连接（Puppeteer/Node.js）**

```typescript
const browser = await puppeteer.connect({ browserWSEndpoint: wsEndpoint });
```

- **结果：** `Target.getBrowserContexts timed out. Increase the 'protocolTimeout' setting...`
- **说明：** WebSocket 连接成功建立，但 CDP 协议层无响应

**测试 3（2026-05-05）：Dashboard Playground 对比**

通过用户浏览器在 `dashboard.novada.com/overview/browser/` 点击 "Run" 执行 Playground 示例：

- **结果：** 控制台显示 `Status running`，**持续超过 2 分钟仍未完成**
- **说明：** Dashboard 本身也无法完成连接，Playground 卡住

#### 关键发现（来自官方文档）

通过读取 `developer.novada.com/novada/advanced-proxy-solutions/browser-api/configure-browser-api` 发现：

> **"one active session per account"（每个账户同时只允许一个活跃会话）**

官方文档原文：
- 每个账户只能维持一个活跃的浏览器会话
- 如果上一个会话没有显式关闭（`browser.close()`），新的连接请求会**无限期挂起**
- 会话 TTL：30 分钟无操作后自动关闭

#### 根本原因分析

| 问题 | 原因 |
|------|------|
| connectOverCDP 超时 | 账户已有一个活跃会话未关闭，新连接被服务端挂起 |
| puppeteer.connect 超时 | 同上 |
| Dashboard Playground 卡住 | 同上——说明不是我们代码的问题，是服务端会话状态问题 |
| 会话是谁创建的 | 可能是之前测试过程中某次连接没有正确关闭，残留在服务端 |

**结论：连接方法（connectOverCDP）是正确的**（官方文档确认）。问题是**服务端存在一个卡死的残留会话，阻塞了所有新连接**。

#### 所需后端操作

**立即操作（解决当前卡死）：**

1. **在后台强制关闭该账户的所有活跃 Browser API 会话**
   - 账户关联 API Key：`<NOVADA_API_KEY_REDACTED>`
   - 操作：在服务端查询并终止该账户所有 active browser session

**中期改进（防止再次发生）：**

2. **提供会话管理 API 端点**，允许开发者主动查询和终止会话，例如：
   ```
   GET  /browser/sessions           → 列出当前活跃会话
   DELETE /browser/sessions/{id}    → 强制终止指定会话
   DELETE /browser/sessions         → 强制终止该账户所有会话
   ```

3. **提供更好的错误反馈：** 当新连接因 "one active session" 而被拒绝时，WebSocket 应立即关闭并返回具体原因（如错误码 4001 + 消息 "Session already active"），而不是让连接无限期挂起

4. **降低残留会话的 TTL：** 文档说 30 分钟，实际测试中超过 2 小时仍未自动清理，请确认实际 TTL 是否生效

---

### 3.4 Web Unblocker（网页解锁）

#### 接口信息
- **端点：** `POST https://webunlocker.novada.com/request`
- **认证：** `Authorization: Bearer NOVADA_WEB_UNBLOCKER_KEY`

#### 测试记录

**第一阶段（2026-04-20）：接口调通，发现格式问题**

- 测试次数：5 次
- 发现的格式问题（已修复）：
  - `js_render` 必须是布尔值 `true`，不能是字符串 `"True"`
  - 路径必须是 `/request`，不能是根路径
  - 认证是 Bearer Token，不是 body 里的 `api_key` 字段
- 修复后状态：✅ 基本功能可用

**第二阶段（2026-05-05）：发现两个后端问题**

#### 问题 A：`response_format` 参数实际只支持 `"html"`

官方文档列出了三种响应格式：`html`、`json`、`markdown`。测试结果：

| response_format 值 | 结果 |
|--------------------|------|
| `"html"` | ✅ 正常返回 |
| `"json"` | ❌ 返回错误码 `11008` |
| `"markdown"` | ❌ 返回错误码 `11008` |

我们的代码已将 `response_format` 硬编码为 `"html"`，暂时规避了此问题。但官方文档中仍有 `json` 和 `markdown` 的说明，会误导使用者。

#### 问题 B：HTTP 状态码全部返回 200，错误隐藏在 JSON body 中

无论请求成功还是失败，HTTP 状态码始终为 `200`。错误信息嵌套在响应 JSON 的内部字段中：

```json
{
  "code": 0,           ← 外层 code（始终为 0，无意义）
  "data": {
    "code": 11008,     ← 真实错误码（隐藏在这里）
    "msg": "xxx",
    "html": null
  }
}
```

这导致：如果调用方只检查 HTTP 状态码（正常做法），会认为请求成功，实际上拿到的是错误响应。我们已在代码中增加内层 `data.code` 的检查，但这属于后端设计问题。

#### 发现的其他问题

- **内部主机名泄露：** 错误响应的 `err_detail` 字段中暴露了内部服务器主机名（如 `proxy-unblock-na.novada.pro`），建议生产环境过滤

#### 所需后端操作

1. **修复 `response_format` 参数：**
   - 选项 A：实现 `json` 和 `markdown` 格式的支持
   - 选项 B：删除文档中对这两种格式的说明，明确只支持 `html`

2. **修复 HTTP 状态码：**
   - API 错误应返回对应 HTTP 错误码（4xx/5xx），而不是全部返回 200
   - 至少对以下情况使用正确状态码：401（认证失败）、403（权限不足）、400（参数错误）

3. **清理 `err_detail` 中的内部主机名**（安全建议，P2 优先级）

---

## 四、核心问题：为什么 Dashboard 能用，API 不能用？

这是本报告最重要的发现，需要后端团队理解并解决。

### 对比矩阵

| API 功能 | Dashboard 使用方式 | 直接 API 使用方式 | 结果 |
|----------|-------------------|-------------------|------|
| 结构化抓取 | Cookie Session → 内部端点（不对外） | Bearer Token → `scraper.novada.com/request` | Dashboard ✅ / API ❌ 11006 |
| SERP 搜索 | Cookie Session → 内部端点 | Bearer Token → `scraperapi.novada.com/search` | Dashboard ✅ / API ❌ 402 |
| 云端浏览器 | 浏览器内 WebSocket（带登录 Cookie） | Node.js WebSocket + CDP | Dashboard ⚠️ 卡死 / API ❌ 超时 |
| Web 解锁 | Cookie Session → 内部调用 | Bearer Token → `webunlocker.novada.com/request` | Dashboard ✅ / API ✅（有格式缺陷） |

### 根本原因

Dashboard 使用的是**内部认证路径（Cookie + Session）**，而外部 API 调用使用的是**Bearer Token 路径**。这两条路径在权限系统中是**独立的**：

- Cookie 路径：Dashboard 用户登录后自动拥有，不需要额外激活
- Bearer Token 路径：需要单独为每个 API Key 开通特定功能权限

**目前的状态：该 API Key 仅激活了 Bearer Token 路径中的基础功能（代理、Web Unblocker），但 Scraper 和 SERP 功能的 Bearer Token 权限未开通。**

这就是为什么 Dashboard 可以工作（Cookie 路径），而 MCP 代码调用失败（Bearer Token 路径权限不足）。

---

## 五、后端修复方案（按优先级排序）

### P0 — 必须立即处理

| # | 问题 | 操作 | 负责人 | 预计工时 |
|---|------|------|--------|----------|
| 1 | Browser API 会话卡死 | 强制清除 API Key `<NOVADA_API_KEY_REDACTED>...` 的所有残留 browser session | fudong | 5 分钟 |
| 2 | Scraper API Bearer 权限 | 为 API Key `<NOVADA_API_KEY_REDACTED>...` 开通 Bearer Token 访问 Scraper API 的权限 | Ethan 审批 / fudong 执行 | 10 分钟 |
| 3 | SERP API Bearer 权限 | 为 API Key `<NOVADA_API_KEY_REDACTED>...` 开通 `scraperapi.novada.com/search` 的查询权限 | Ethan 审批 / fudong 执行 | 10 分钟 |

### P1 — 短期内处理（影响开发者体验）

| # | 问题 | 操作 | 负责人 | 预计工时 |
|---|------|------|--------|----------|
| 4 | Web Unblocker `response_format` | 修复或删除 `json`/`markdown` 格式支持的文档说明 | fudong | 1 小时 |
| 5 | HTTP 状态码 200-always | Web Unblocker 应返回正确 HTTP 状态码 | fudong | 2 小时 |
| 6 | Browser API 错误反馈 | 会话占用时应立即断开 WS 并返回具体错误，不要无限挂起 | fudong | 4 小时 |
| 7 | SERP 文档 404 | 修复 `Making Request` 页面（404） | 文档团队 | 30 分钟 |

### P2 — 中期改进（提升可维护性）

| # | 问题 | 操作 | 预计工时 |
|---|------|------|----------|
| 8 | Browser 会话管理 API | 提供 REST 接口供开发者查询/终止自己的 browser session | 1 天 |
| 9 | err_detail 主机名过滤 | Web Unblocker 错误中过滤内部主机名 | 2 小时 |
| 10 | Bearer / Cookie 权限对齐 | 审查是否所有 Cookie 路径的功能都对应有 Bearer 路径的开通方式 | 半天 |

---

## 六、验证方法（修复后如何确认）

后端操作完成后，可用以下命令验证：

### 验证 Scraper API 是否修复

```bash
curl -X POST https://scraper.novada.com/request \
  -H "Authorization: Bearer <NOVADA_API_KEY_REDACTED>" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "scraper_name=amazon.com&scraper_id=amazon_product_by-keywords&keyword=iPhone"
```

**预期响应（修复后）：**
```json
{ "code": 0, "data": { "code": 200, "data": { "task_id": "xxx" } } }
```

**当前响应（未修复）：**
```json
{ "code": 11006, "msg": "Scraper error", "data": null }
```

### 验证 SERP API 是否修复

```bash
curl "https://scraperapi.novada.com/search?engine=google&q=iPhone&api_key=<NOVADA_API_KEY_REDACTED>"
```

**预期响应（修复后）：**
```json
{ "code": 0, "data": { "organic_results": [...] } }
```

**当前响应（未修复）：**
```json
{ "code": 402, "msg": "Api Key error：User has no permission" }
```

### 验证 Browser API 会话是否已清除

```javascript
// Node.js 测试脚本
const { chromium } = require("playwright-core");
const browser = await Promise.race([
  chromium.connectOverCDP("wss://user:pass@upg-scbr.novada.com"),
  new Promise((_, rej) => setTimeout(() => rej(new Error("timeout")), 15000))
]);
console.log("连接成功:", browser.isConnected());
await browser.close();
```

**预期（修复后）：** 在 15 秒内连接成功，打印 `连接成功: true`  
**当前状态：** 15 秒后超时，或输出 `Target.getBrowserContexts timed out`

---

## 七、附录：我们已完成的代码修复

以下问题**不需要后端介入**，我们在 MCP 代码层已自行修复：

| 修复项 | 文件 | 内容 |
|--------|------|------|
| 防止 Browser API 失败导致 extract/unblock 崩溃 | `src/utils/router.ts` | 所有 `fetchViaBrowser` 调用加 try/catch，失败时优雅降级 |
| Browser API 超时错误信息 | `src/utils/browser.ts` | 超时错误现在包含 `agent_instruction`，告诉 Agent 下一步操作 |
| Scraper 11006 错误描述不准确 | `src/tools/scrape.ts` | 修正为"Bearer Token 权限未开通，与 Dashboard Cookie 认证是两个不同层级" |
| Web Unblocker 内层错误码未检查 | `src/utils/http.ts` | 新增 `data.code !== 200` 检查，避免错误被静默吞掉 |
| `response_format` 参数 | `src/tools/types.ts` | 参数从未暴露给调用方，代码中硬编码为 `"html"`，已规避 |

---

## 八、联系与跟进

- **MCP 集成方：** tongwu
- **Novada 后端对接：** fudong（技术）、Ethan（审批）
- **优先级最高的两项操作：**
  1. 清除 Browser API 卡死会话（5 分钟，fudong 可直接操作）
  2. 为 API Key `<NOVADA_API_KEY_REDACTED>` 开通 Scraper + SERP Bearer 权限

完成上述两项操作后，请通知 tongwu 进行验证测试。

---

## 九、架构建议：一次性根本解决方案

### 当前问题的本质

逐个 API Key 手动激活权限不是最终解法——它只是治标，每新增一个客户或 API Key 都需要重复操作，长期会成为运营负担。

当前权限模型存在根本性的设计裂缝：

```
用户账户
├── Cookie Session 权限（Dashboard 使用）
│   └── Scraper ✅  SERP ✅  Browser ✅  ← 开通订阅后自动拥有
│
└── Bearer Token 权限（API 直接调用）
    └── Scraper ❌  SERP ❌              ← 需要额外手动激活，不自动继承
```

**结果：** 同一个用户，在 Dashboard 上能用的功能，通过 API 调用却失败。这对开发者来说是完全无法理解的体验断层，也是目前所有问题的共同根源。

---

### 推荐方案：统一权限来源（Option B）

**核心思路：** Bearer Token 的可访问功能 = 该用户账户的订阅功能。两者使用同一套权限数据源，不再维护两套独立的激活状态。

**改造逻辑：**

```
改造前（当前）：
  验证 Bearer Token
    → 查询 bearer_token_permissions 表
    → 检查是否单独激活了 scraper_access = true
    → ❌ 未激活 → 返回 11006

改造后（目标）：
  验证 Bearer Token
    → 解析出 user_id / account_id
    → 查询该账户的订阅套餐（与 Dashboard Cookie 用同一张表）
    → 检查套餐是否包含 scraper 功能
    → ✅ 包含 → 正常处理请求
```

---

### 实施步骤

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 审查现有权限数据结构 | 确认 Cookie Session 和 Bearer Token 当前分别查哪张表/哪个字段 |
| 2 | 确定"功能权限"的统一来源 | 通常是用户订阅套餐表（`subscriptions` 或 `account_products`） |
| 3 | 修改 Bearer Token 鉴权中间件 | 将功能检查改为查订阅套餐，而不是独立的 bearer 权限表 |
| 4 | 回归测试 | 确认 Dashboard（Cookie）和 API（Bearer）对同一用户返回一致结果 |
| 5 | 清理旧逻辑 | 删除或归档手动激活 bearer_token_permissions 的相关代码和入口 |

**预估工时：** 3–5 个工作日（含测试）  
**一次性：** ✅ 修改后对所有现有和未来用户永久生效，无需再手动激活

---

### 过渡期处理（实施期间）

在 Option B 完成之前，建议运行一次性迁移脚本作为临时措施：

```sql
-- 伪代码：将所有已在 Dashboard 激活某产品的账户，批量同步 Bearer 权限
INSERT INTO bearer_token_permissions (api_key, product, activated_at)
SELECT ak.key, sp.product_name, NOW()
FROM api_keys ak
JOIN account_products sp ON ak.account_id = sp.account_id
WHERE sp.is_active = true
ON CONFLICT DO NOTHING;
```

这个脚本运行一次，解决所有存量账户（包括 `<NOVADA_API_KEY_REDACTED>...`），之后 Option B 上线后即可废弃。

---

### 为什么不选 Option A（逐个激活脚本）

| 维度 | Option A（脚本逐个激活） | Option B（统一权限源） |
|------|--------------------------|------------------------|
| 实施工时 | 1 天 | 3–5 天 |
| 是否永久解决 | ❌ 新客户仍需手动操作 | ✅ 一次性 |
| 运营负担 | 持续存在 | 消除 |
| 开发者体验 | 仍有激活断层 | Dashboard 和 API 完全一致 |
| 扩展性 | 差 | 好 |

Option A 是临时补丁，Option B 是根本修复。建议 Ethan 审批 Option B 的开发排期，同时用 Option A 的过渡脚本先解锁测试账号。

---

## 十、联系与跟进（更新）

- **MCP 集成方：** tongwu
- **Novada 后端对接：** fudong（技术）、Ethan（审批）

**立即操作（今天，fudong）：**
1. 清除 Browser API 卡死会话
2. 运行过渡脚本，为 API Key `<NOVADA_API_KEY_REDACTED>` 批量同步权限

**排期讨论（本周，Ethan）：**
3. 审批 Option B（统一权限模型）开发排期，彻底解决所有客户的 API 访问一致性问题

完成立即操作后，请通知 tongwu 进行验证测试。

---

*报告生成时间：2026-05-05 | 基于 2 次深度测试周期（2026-04-20 和 2026-05-05）*
